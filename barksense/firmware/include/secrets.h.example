#pragma once

// Fill in your credentials. This file is gitignored — never commit it.
constexpr const char* kWifiSsid = "YOUR_WIFI_SSID";
constexpr const char* kWifiPass = "YOUR_WIFI_PASSWORD";

// Create a topic at ntfy.sh (free, no account needed), subscribe in the app.
constexpr const char* kNtfyUrl = "https://ntfy.sh/YOUR_TOPIC";
